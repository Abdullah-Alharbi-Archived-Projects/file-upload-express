const MS = 1000;
const MINUTE = MS * 60; // 60 SECONDS = 1 MINUTE
const HOUR = MINUTE * 60; // 60 MINUTES = 1 HOUR
const DAY = MINUTE * 24; // 24 HOURS = 1 DAY

module.exports = {
  MS,
  MINUTE,
  HOUR,
  DAY
};
